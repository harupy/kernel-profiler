version: "3"
services:
  kernel-profiler:
    build:
      context: .
      dockerfile: Dockerfile.ci
    volumes:
      - .:/kernel-profiler
    image: kernel-profiler-image
    container_name: kernel-profiler-container
